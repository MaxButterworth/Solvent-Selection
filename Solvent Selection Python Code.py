import pandas as pdimport numpy as npfrom rdkit import Chemfrom rdkit import DataStructsimport plotly as px# Opening the reaction databasedatabase = pd.read_csv(r'/Users/maxbutterworth/Library/CloudStorage/OneDrive-TheUniversityofNottingham/Nottingham/Year 3/Synoptic Module/Research Project/Solvent-Selection/Database.csv')database = database.to_numpy() # Assigning Pandas database to numpy array# Defining category arraysreagents = database[0:, 0]solvents = database[0:, 1]smiles = database[0:, 6]print(smiles)# Setting up the SMILES fingerprints#ms = [Chem.MolFromSmiles(smiles[i]) for i in range(0, len(smiles))]#fps = [Chem.RDKFingerprint(j) for j in smiles]# Defining a function to search the reaction#def reaction_search(input_smiles):        #print(database)