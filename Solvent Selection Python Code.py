import pandas as pdimport numpy as npfrom rdkit import Chemfrom rdkit import DataStructsimport plotly as px# Opening the reaction databasedatabase = pd.read_csv(r'/Users/maxbutterworth/Library/CloudStorage/OneDrive-TheUniversityofNottingham/Nottingham/Year 3/Synoptic Module/Research Project/Solvent-Selection/Database copy.csv')database = database.to_numpy() # Assigning Pandas database to numpy array# Defining category arraysreagents = database[0:, 0]solvents = database[0:, 1]smiles = database[0:, 6]ms = np.zeros(len(smiles)) # Defining the molecular SMILES arrayfps = np.zeros(len(smiles)) # Defining the fingerprints array#print(smiles)# Setting up the SMILES fingerprintsfor i in range(0, len(smiles)):    ms[i] = Chem.MolFromSmiles(f'{smiles[i]}')    fps[i] = Chem.RDKFingerprint(f'{smiles[i]}')#print(fps)    # Defining a function to search the reaction#def reaction_search(input_smiles):        #print(database)